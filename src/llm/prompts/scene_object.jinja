{# 场景对象生成 / Scene Object Generation #}
为「{{ world_name }}」世界生成 {{ count }} 个场景对象。

{% if theme %}
世界主题：{{ theme }}
{% endif %}

关键约束：
- scene_id 只能使用 scene_1（只有 1 个场景）
- object_type 从以下选：chest, door, trap, mechanism, decoration
- 至少一个宝箱(chest)包含物品掉落

输出格式（严格 YAML）：
# scene_object_1
id: scene_object_1
name: 远古石门
object_type: door
scene_id: scene_1
interact_data:
  locked: true
  dc: 15

{# 输出恰好 {{ count }} 个 #}
