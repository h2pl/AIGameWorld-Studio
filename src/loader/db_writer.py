# SQLite 写入 / SQLite Writer
# Pydantic 模型 → SQLite 15 表，6 表同一事务 / Pydantic → SQLite 15 tables, single transaction

# TODO: 实现 write_template() + clear_existing() / Implement write_template() + clear_existing()
